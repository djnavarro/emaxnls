% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emaxlogistic-methods.R, R/emaxnls-methods.R
\name{AIC.emaxlogistic}
\alias{AIC.emaxlogistic}
\alias{BIC.emaxlogistic}
\alias{AIC}
\alias{AIC.emaxnls}
\alias{BIC.emaxnls}
\title{Akaike information criterion / Bayesian information criterion}
\usage{
\method{AIC}{emaxlogistic}(object, ..., k = 2)

\method{BIC}{emaxlogistic}(object, ...)

\method{AIC}{emaxnls}(object, ..., k = 2)

\method{BIC}{emaxnls}(object, ...)
}
\arguments{
\item{object}{An \code{emaxnls} or \code{emaxlogistic} object}

\item{...}{Optionally, more fitted model objects}

\item{k}{Penalty per parameter in the AIC}
}
\value{
If just one object is provided, a numeric value with the corresponding AIC (or BIC).
If multiple objects are provided, a data.frame with rows corresponding to the objects
and columns representing the number of parameters in the model (\code{df}) and the AIC or BIC.
}
\description{
Computes AIC or BIC for one or more fitted Emax models. Lower values
indicate a better-fitting model; values are only meaningful in comparison
to other models fitted to the same response variable and dataset.
}
\details{
AIC applies a penalty of \code{2 * k} to minus twice the log-likelihood, where
\code{k} is the number of estimated parameters. BIC applies \code{log(n) * k},
making it more conservative than AIC in large samples. When multiple models
are passed, any non-converging models are dropped with a warning.
}
\examples{
mod_0 <- emax_nls(
  structural_model = rsp_1 ~ exp_1,
  covariate_model = list(E0 ~ 1, Emax ~ 1, logEC50 ~ 1),
  data = emax_df
)
mod_1 <- emax_nls(
  structural_model = rsp_1 ~ exp_1,
  covariate_model = list(E0 ~ cnt_a, Emax ~ 1, logEC50 ~ 1),
  data = emax_df
)

# calculate AIC for individual models
AIC(mod_0)
AIC(mod_1)

# calculate AIC for a sequence of models
AIC(mod_0, mod_1)

# calculate BIC for individual models
BIC(mod_0)
BIC(mod_1)

# calculate BIC for a sequence of models
BIC(mod_0, mod_1)

# emaxlogistic models
mod_b0 <- emax_logistic(
  structural_model = rsp_2 ~ exp_1,
  covariate_model = list(E0 ~ 1, Emax ~ 1, logEC50 ~ 1),
  data = emax_df
)
mod_b1 <- emax_logistic(
  structural_model = rsp_2 ~ exp_1,
  covariate_model = list(E0 ~ cnt_a, Emax ~ 1, logEC50 ~ 1),
  data = emax_df
)
AIC(mod_b0, mod_b1)
BIC(mod_b0, mod_b1)

}
