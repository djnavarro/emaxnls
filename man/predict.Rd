% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emaxlogistic-methods.R, R/emaxnls-methods.R
\name{predict.emaxlogistic}
\alias{predict.emaxlogistic}
\alias{predict}
\alias{predict.emaxnls}
\title{Predicting from Emax regression models}
\usage{
\method{predict}{emaxlogistic}(
  object,
  newdata = NULL,
  type = c("response", "link"),
  se.fit = FALSE,
  interval = "none",
  level = 0.95,
  ...
)

\method{predict}{emaxnls}(
  object,
  newdata = NULL,
  se.fit = FALSE,
  interval = "none",
  level = 0.95,
  ...
)
}
\arguments{
\item{object}{An \code{emaxnls} or \code{emaxlogistic} object}

\item{newdata}{A named list or data frame in which to look for variables with which to predict.
If \code{newdata} is missing the fitted values at the original data points are returned.}

\item{type}{For \code{emaxlogistic} objects: \code{"response"} (default) returns predicted
probabilities; \code{"link"} returns the linear predictor on the logit scale. Ignored
for \code{emaxnls} objects.}

\item{se.fit}{A switch indicating if standard errors are required.}

\item{interval}{A character string indicating if prediction intervals or a confidence
interval on the mean responses are to be calculated. Can be \code{"none"}, \code{"confidence"},
or \code{"prediction"}.}

\item{level}{A numeric scalar between 0 and 1 giving the confidence level for the
intervals (if any) to be calculated.}

\item{...}{Ignored}
}
\value{
The return value differs slightly depending on inputs. When \code{se.fit = FALSE},
it produces a vector or matrix of predictions with column names \code{fit}, \code{lwr} and \code{upr}
if the \code{interval} argument is set. When \code{se.fit = TRUE}, it returns a list with the
following components:
\itemize{
\item \code{fit}: vector or matrix as above
\item \code{se.fit}: standard error of the predicted means
\item \code{residual.scale}: residual standard deviation
\item \code{df}: residual degrees of freedom
}
}
\description{
Generates predictions from a fitted Emax model, either at the original data
points or at new covariate and exposure values supplied via \code{newdata}.
Standard errors and confidence or prediction intervals can be requested.
}
\details{
For \code{emaxlogistic} objects, when \code{interval} is set, the bounds are first
computed on the link scale and then passed through the inverse logit
transformation, ensuring they remain in the unit interval on the
probability scale.
}
\examples{
mod_c <- emax_nls(
  structural_model = rsp_1 ~ exp_1,
  covariate_model = list(E0 ~ cnt_a, Emax ~ 1, logEC50 ~ 1),
  data = emax_df
)

# return a vector of predictions
predict(mod_c)[1:20]

# return a matrix with confidence intervals
predict(mod_c, interval = "confidence", se.fit = FALSE)

# emaxlogistic predicted probabilities
mod_b <- emax_logistic(
  structural_model = rsp_2 ~ exp_1,
  covariate_model = list(E0 ~ cnt_a, Emax ~ 1, logEC50 ~ 1),
  data = emax_df
)
predict(mod_b)[1:20]
predict(mod_b, type = "link")[1:20]

}
