% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emaxlogistic-methods.R, R/emaxnls-methods.R
\name{anova.emaxlogistic}
\alias{anova.emaxlogistic}
\alias{anova}
\alias{anova.emaxnls}
\title{Analysis of variance for Emax regression models}
\usage{
\method{anova}{emaxlogistic}(object, ...)

\method{anova}{emaxnls}(object, ...)
}
\arguments{
\item{object}{An \code{emaxnls} or \code{emaxlogistic} object}

\item{...}{Additional fitted model objects of the same class}
}
\value{
For \code{emaxnls} objects, an analysis of variance table for a
sequence of models. For \code{emaxlogistic} objects, a data frame with
columns \code{Df}, \code{Deviance}, \code{Df_diff}, \code{LRT}, and \verb{Pr(>Chi)}.
}
\description{
Compares a sequence of nested Emax models. At least two model objects must
be provided; all must be of the same class.
}
\details{
For \code{emaxnls} objects, calls \code{stats::anova()} on the underlying \code{nls}
objects to produce an ANOVA table for the sequence of models. For
\code{emaxlogistic} objects, computes a likelihood ratio chi-squared test
comparing nested models; the test statistic is the difference in
deviances and the reference distribution is chi-squared with degrees
of freedom equal to the difference in the number of parameters. The
nesting assumption is not checked; results are only interpretable when
each successive model genuinely adds parameters to the previous one.
}
\examples{
mod_0 <- emax_nls(
  structural_model = rsp_1 ~ exp_1,
  covariate_model = list(E0 ~ 1, Emax ~ 1, logEC50 ~ 1),
  data = emax_df
)
mod_1 <- emax_nls(
  structural_model = rsp_1 ~ exp_1,
  covariate_model = list(E0 ~ cnt_a, Emax ~ 1, logEC50 ~ 1),
  data = emax_df
)
anova(mod_0, mod_1)

# emaxlogistic: likelihood ratio test
mod_b0 <- emax_logistic(
  structural_model = rsp_2 ~ exp_1,
  covariate_model = list(E0 ~ 1, Emax ~ 1, logEC50 ~ 1),
  data = emax_df
)
mod_b1 <- emax_logistic(
  structural_model = rsp_2 ~ exp_1,
  covariate_model = list(E0 ~ cnt_a, Emax ~ 1, logEC50 ~ 1),
  data = emax_df
)
anova(mod_b0, mod_b1)

}
