% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{emax_logistic}
\alias{emax_logistic}
\title{Estimate parameters for a logistic Emax regression model}
\usage{
emax_logistic(
  structural_model,
  covariate_model,
  data,
  init = NULL,
  opts = NULL
)
}
\arguments{
\item{structural_model}{A two-sided formula of the form response ~ exposure}

\item{covariate_model}{A list of two-sided formulas, each specifying a
covariate model for a structural parameter}

\item{data}{A data frame that includes all relevant variables}

\item{init}{Initial values and bounds for parameters. See \code{emax_logistic_init()}}

\item{opts}{Model fitting and optimization options. See \code{emax_logistic_options()}}
}
\value{
An object of class \code{emaxlogistic} (which also inherits from \code{emaxnls})
}
\description{
Fits a logistic Emax regression model for a binary response variable using
iterative reweighted least squares (IRLS). For continuous outcomes, use
\code{emax_nls()} instead.
}
\details{
The structural Emax model is placed on the log-odds (logit) scale:

\code{logit(p) = E0 + Emax * x / (x + EC50)}  (hyperbolic)

\code{logit(p) = E0 + Emax * x^h / (x^h + EC50^h)}  (sigmoidal)

Estimation uses iterative reweighted least squares (IRLS). At each outer
iteration a weighted NLS problem is solved using working weights and a working
response derived from the current parameter estimates. This is equivalent to
Fisher scoring and produces maximum likelihood estimates at convergence.

The interface mirrors \code{emax_nls()} exactly: the \code{structural_model} and
\code{covariate_model} arguments have the same specification, including support
for sigmoidal models via a \code{logHill} term. The response variable in
\code{structural_model} must be a binary (0/1) numeric vector.
}
\examples{
emax_logistic(
  structural_model = rsp_2 ~ exp_1,
  covariate_model = list(E0 ~ cnt_a, Emax ~ 1, logEC50 ~ 1),
  data = emax_df
)

}
\seealso{
\code{emax_logistic_options()}, \code{emax_logistic_init()}, \code{emax_nls()}
}
