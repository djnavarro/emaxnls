% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emaxlogistic-methods.R, R/emaxnls-methods.R
\name{residuals.emaxlogistic}
\alias{residuals.emaxlogistic}
\alias{residuals}
\alias{residuals.emaxnls}
\title{Residuals for an Emax regression model}
\usage{
\method{residuals}{emaxlogistic}(object, type = c("pearson", "deviance"), ...)

\method{residuals}{emaxnls}(object, ...)
}
\arguments{
\item{object}{An \code{emaxnls} or \code{emaxlogistic} object}

\item{type}{For \code{emaxlogistic} objects: the type of residuals to return.
\code{"pearson"} (default) returns Pearson residuals; \code{"deviance"} returns
deviance residuals. Ignored for \code{emaxnls} objects.}

\item{...}{Ignored}
}
\value{
A numeric vector of residuals
}
\description{
For \code{emaxnls} objects, returns raw residuals on the response scale. For
\code{emaxlogistic} objects, Pearson or deviance residuals are available via
the \code{type} argument.
}
\details{
Pearson residuals are the raw residuals divided by \code{sqrt(mu * (1 - mu))},
the estimated standard deviation of a Bernoulli observation, giving a
standardized measure of discrepancy. Deviance residuals are the signed
square root of each observation's contribution to the total binomial
deviance; their sum of squares equals the model deviance returned by
\code{deviance()}.
}
\examples{
mod_c <- emax_nls(
  structural_model = rsp_1 ~ exp_1,
  covariate_model = list(E0 ~ cnt_a, Emax ~ 1, logEC50 ~ 1),
  data = emax_df,
  opts = emax_nls_options(max_time = 10)
)
residuals(mod_c)[1:20]

mod_b <- emax_logistic(
  structural_model = rsp_2 ~ exp_1,
  covariate_model = list(E0 ~ cnt_a, Emax ~ 1, logEC50 ~ 1),
  data = emax_df,
  opts = emax_logistic_options(max_time = 10)
)
residuals(mod_b)[1:20]
residuals(mod_b, type = "deviance")[1:20]

}
