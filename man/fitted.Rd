% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emaxlogistic-methods.R, R/emaxnls-methods.R
\name{fitted.emaxlogistic}
\alias{fitted.emaxlogistic}
\alias{fitted}
\alias{fitted.emaxnls}
\title{Fitted values for an Emax regression model}
\usage{
\method{fitted}{emaxlogistic}(object, type = c("response", "link"), ...)

\method{fitted}{emaxnls}(object, ...)
}
\arguments{
\item{object}{An \code{emaxnls} or \code{emaxlogistic} object}

\item{type}{For \code{emaxlogistic} objects: \code{"response"} (default) returns
fitted probabilities; \code{"link"} returns the linear predictor on the
logit scale. Ignored for \code{emaxnls} objects.}

\item{...}{Ignored}
}
\value{
A numeric vector of fitted values, with length equal to the number
of observations in the original data
}
\description{
Returns the model predictions at the original data points. For
\code{emaxlogistic} objects, the \code{type} argument controls whether fitted
probabilities or the linear predictor on the logit scale are returned.
}
\details{
For \code{emaxnls} objects, these are the predicted values from the Emax curve
evaluated at each observation's exposure and covariate values.
}
\examples{
mod_c <- emax_nls(
  structural_model = rsp_1 ~ exp_1,
  covariate_model = list(E0 ~ cnt_a, Emax ~ 1, logEC50 ~ 1),
  data = emax_df
)
fitted(mod_c)[1:20]

mod_b <- emax_logistic(
  structural_model = rsp_2 ~ exp_1,
  covariate_model = list(E0 ~ cnt_a, Emax ~ 1, logEC50 ~ 1),
  data = emax_df
)
fitted(mod_b)[1:20]
fitted(mod_b, type = "link")[1:20]

}
